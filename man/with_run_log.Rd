% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_local.R
\name{with_run_log}
\alias{with_run_log}
\title{Collect a Function's Messages and Warnings Into a Log}
\usage{
with_run_log(expr, echo = TRUE)
}
\arguments{
\item{expr}{The expression to evaluate.}

\item{echo}{Logical. Also print each line as it arrives.}
}
\value{
A list with \code{value} and \code{log}.
}
\description{
The pipeline's \code{message()} output is its progress report and its
diagnostics. Captured rather than muffled, it becomes the run log; captured
rather than printed, a \code{verbose} run does not bury the console.
}
\details{
Warnings are collected too, prefixed so they stand out, and are \emph{not}
re-raised: a run that skipped three variables should say so once in the log,
not fire three warnings at the end when it is too late to read them in
order.
}
\keyword{internal}
