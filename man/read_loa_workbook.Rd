% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{read_loa_workbook}
\alias{read_loa_workbook}
\title{Read a List of Analysis Workbook}
\usage{
read_loa_workbook(path, filename = NULL)
}
\arguments{
\item{path}{Path to a \code{.xlsx} or \code{.csv} file.}

\item{filename}{Optional original filename, for messages. Defaults to
\code{basename(path)}.}
}
\value{
A list with \code{sheets} (named list of data frames), \code{format},
\code{filename}, \code{sheet_names} (as written in the workbook), \code{unknown_sheets}
and \code{ignored_sheets}.
}
\description{
Reads every recognised sheet and interprets nothing. Unrecognised sheet
names are recorded rather than rejected here, so that
\code{\link{validate_loa}} remains the single place that decides what is a
problem.
}
\details{
Configuration sheets are read as text so their types are decided by this
file rather than by whatever \pkg{readxl} guessed from the first few rows.
The \code{analysis} sheet keeps its guessed types, so a numeric \code{level} column
stays numeric.

A \code{.csv} upload carries no sheets: it is read as the \code{analysis} table alone,
and \code{format} is \code{"CSV"} so the caller can say so.
}
