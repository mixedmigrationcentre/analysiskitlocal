% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_nonempty_blocks}
\alias{ck_nonempty_blocks}
\title{Which Group Blocks Actually Have Respondents on This Sheet}
\usage{
ck_nonempty_blocks(dat, rows, blocks, idx, sample_base = NULL)
}
\arguments{
\item{dat}{The table.}

\item{rows}{The sheet's row indices.}

\item{blocks}{All statistic blocks.}

\item{idx}{The block indices assigned to this sheet.}

\item{sample_base}{Base name of the denominator column (e.g. \code{"n_total"}),
or \code{NULL}.}
}
\value{
The subset of \code{idx} worth writing. If every block would go, \code{idx} is
returned unchanged rather than producing a sheet with no columns.
}
\description{
A grouping variable's levels are fixed across the whole table, so a country
with no interviews for the questions on one sheet still gets a full block of
columns there - every cell empty, or \verb{#NUM!} where the estimator divided
nothing by nothing. Those blocks are dropped.
}
\details{
A block counts as having respondents when its denominator column holds a
finite value above zero somewhere in the sheet's rows. With no count column
to read, the fallback is whether any statistic in the block is finite at all;
\code{NaN} and \code{Inf} are not, which is exactly the case being removed.

The Overall block is never dropped - it is the reference column, and dropping
it would leave a sheet with nothing to compare against.
}
\keyword{internal}
