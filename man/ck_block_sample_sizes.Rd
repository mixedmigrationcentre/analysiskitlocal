% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_block_sample_sizes}
\alias{ck_block_sample_sizes}
\title{Sample Size of Each Group Block}
\usage{
ck_block_sample_sizes(dat, blocks, sample_base, rows = NULL)
}
\arguments{
\item{dat}{The table.}

\item{blocks}{The statistic blocks.}

\item{sample_base}{Base name of the denominator column (e.g. \code{"n_total"}), or
\code{NULL} when the table carries no count column.}

\item{rows}{Optional row indices to restrict to. Defaults to the whole table.}
}
\value{
A numeric vector, one element per block, \code{NA} where no finite
denominator was recorded.
}
\description{
The number of respondents behind a block is the largest denominator recorded
for it across the rows considered - the same rule the block headers and the
readme composition table use, so the three always agree.
}
\keyword{internal}
