% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_pipeline.R
\name{ck_check_choice_combinations}
\alias{ck_check_choice_combinations}
\title{Check a count_combinations Specification}
\usage{
ck_check_choice_combinations(
  combinations,
  dataset,
  loa = NULL,
  sm_separator = "/",
  ignore_case = TRUE,
  exclude_choices = NULL,
  max_choices = 6,
  arg_name = "count_combinations"
)
}
\arguments{
\item{combinations}{The \code{count_combinations} specification.}

\item{dataset}{The dataset (used for the child columns).}

\item{loa}{The list of analyses (used for declared types).}

\item{sm_separator}{Separator between parent and choice. Default \code{"/"}.}

\item{ignore_case}{Logical. Match labels case-insensitively.}

\item{exclude_choices}{The run's \code{exclude_choices}, if any.}

\item{max_choices}{Refuse more than this many focus choices per question, so
a long list cannot silently produce hundreds of rows. Default \code{6}
(64 combinations).}

\item{arg_name}{The pipeline argument being checked, used in the error text.}
}
\value{
The normalised specification, invisibly: a named list of named
character vectors, names being the display labels. Errors otherwise.
}
\description{
Validates the whole specification before any work is done, so a mistyped
choice label is the first thing the user sees rather than a wrong table.
Checks the structure, that every question really is a select_multiple, that
every choice label resolves to a child column, and that no focus choice is
also in \code{exclude_choices} (which would be contradictory: one argument
asks to report on a choice, the other to drop everyone who picked it).
}
