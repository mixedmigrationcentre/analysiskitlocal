% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_pipeline.R
\name{ck_group_sums}
\alias{ck_group_sums}
\title{Group-Wise Sums of Several Numeric Vectors at Once}
\usage{
ck_group_sums(x, g, levels_g)
}
\arguments{
\item{x}{A numeric matrix (or vector) whose rows align with \code{g}.
Column names become the column names of the result.}

\item{g}{Group vector (character).}

\item{levels_g}{Group levels to report, in order.}
}
\value{
A numeric matrix: one row per level of \code{levels_g}, one column
per column of \code{x}. Levels with no rows are zero.
}
\description{
The same arithmetic as calling \code{\link{ck_gsum}} once per vector, but a
single \code{rowsum()} pass over a matrix instead of one per statistic. Each
analysis needs two to four group-wise sums over the same grouping, so this is
where the repetition was.
}
\keyword{internal}
