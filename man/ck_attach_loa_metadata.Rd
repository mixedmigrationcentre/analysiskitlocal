% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_pipeline.R
\name{ck_attach_loa_metadata}
\alias{ck_attach_loa_metadata}
\title{Attach LOA Metadata Columns to a Results Table}
\usage{
ck_attach_loa_metadata(results_table, loa, extra_columns = NULL)
}
\arguments{
\item{results_table}{A results table with \code{analysis_type} and
\code{analysis_var}.}

\item{loa}{The original LOA (before stacking).}

\item{extra_columns}{LOA columns to carry through.}
}
\value{
\code{results_table} with the metadata columns at the front.
}
\description{
Joins DAP columns such as \code{sector} or \code{indicator} onto the results.
Deduplicated first and explicitly many-to-one, so a DAP with more than one
row per analysis_type + analysis_var cannot silently multiply result rows.
Run after the pivot, so it can never split rows.
}
