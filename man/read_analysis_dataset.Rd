% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_dataset.R
\name{read_analysis_dataset}
\alias{read_analysis_dataset}
\title{Read a Dataset from a Path}
\usage{
read_analysis_dataset(path)
}
\arguments{
\item{path}{Path to a \code{.csv} or \code{.xlsx} file. Only the first sheet of an
\code{.xlsx} is read.}
}
\value{
A list with \code{data}, \code{extension}, \code{filename} and \code{size}.
}
\description{
Reads a \code{.csv} or \code{.xlsx} research dataset and reports what it found. The
file is read as it is: \strong{row 1 of an ONA export is the label row and is
left in place}, because that is what \code{\link[=build_analysis_spec]{build_analysis_spec()}} and
\code{\link[=run_analysis_spec]{run_analysis_spec()}} expect. The label row is set aside by the pipeline
itself, under the \code{skip_label_row} setting.
}
\details{
CSV blanks and the strings \code{NA}, \code{N/A} and \code{NULL} are read as missing.
Column names are preserved exactly (\code{check.names = FALSE}), because the
select_multiple child columns carry choice labels that R would otherwise
mangle.
}
\examples{
path <- tempfile(fileext = ".csv")
utils::write.csv(
  data.frame(Q27 = c("What is your gender?", "Female", "Male")),
  path,
  row.names = FALSE
)

dataset <- read_analysis_dataset(path)
dataset_overview(dataset)
}
\seealso{
\code{\link[=dataset_overview]{dataset_overview()}} and \code{\link[=dataset_columns]{dataset_columns()}} to inspect the result,
\code{\link[=read_loa_workbook]{read_loa_workbook()}} for the other half of a run.
}
