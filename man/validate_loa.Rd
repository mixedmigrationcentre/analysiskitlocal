% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{validate_loa}
\alias{validate_loa}
\title{Validate a List of Analysis Workbook}
\usage{
validate_loa(workbook, dataset = NULL)
}
\arguments{
\item{workbook}{The list returned by \code{\link{read_loa_workbook}}, or a
bare named list of sheets.}

\item{dataset}{Optional dataset, with the ONA label row still on top. When
supplied, variable-existence and column-ownership checks are run.}
}
\value{
A data frame with \code{sheet}, \code{row}, \code{severity} and \code{message}.
}
\description{
Every check the schema defines, in one place. Never stops: it returns the
problems so the app can show all of them at once instead of one per run.
}
\details{
The severity rule, applied throughout:
\strong{fatal} when the run would produce wrong or misleading output; \strong{warning}
when it would only produce less output.
}
