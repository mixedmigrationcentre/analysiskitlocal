% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_write_block_sheet}
\alias{ck_write_block_sheet}
\title{Write One Question-Block Sheet}
\usage{
ck_write_block_sheet(
  wb,
  sheet,
  dat,
  blocks,
  block_group,
  value_columns,
  total_columns,
  pal,
  font_name,
  order_per_question = TRUE,
  colour_scale = TRUE,
  max_colour_scale_rules = 250,
  category_width = 46,
  group_width = 14,
  pct_fmt = ck_percent_format(0)
)
}
\arguments{
\item{wb}{An openxlsx workbook.}

\item{sheet}{Sheet name (the sheet is created here).}

\item{dat}{The rows for this sheet.}

\item{blocks}{Blocks to show, in column order.}

\item{block_group}{Grouping variable of each block.}

\item{value_columns}{Statistic column prefixes; the first is the one shown.}

\item{total_columns}{Count column prefixes.}

\item{pal}{Resolved MMC palette.}

\item{font_name}{Font used throughout.}

\item{order_per_question}{Logical. Order each question's groups by its own
denominator, largest first, instead of following the sheet-wide order.}

\item{colour_scale}{Logical, shade the percentage block of each question.}

\item{max_colour_scale_rules}{Rule ceiling for the colour scale.}

\item{category_width}{Width of the category columns.}

\item{group_width}{Width of the group value columns.}

\item{pct_fmt}{Excel number format for proportion rows, from
\code{\link[=ck_percent_format]{ck_percent_format()}}.}
}
\value{
Invisibly the number of style objects written.
}
\description{
Each question becomes a small table: the question label on top, the
disaggregation variable band under it, then a header row naming every group
with its sample size, then one row per answer category. Percentages sit on
the left panel and the matching counts on the right, separated by a spacer
column, so a figure and its count can be read without scrolling.
}
\keyword{internal}
