% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{ak_install_missing}
\alias{ak_install_missing}
\title{Install a Set of Packages, Best Effort}
\usage{
ak_install_missing(specs, upgrade = FALSE)
}
\arguments{
\item{specs}{Install specs from \code{\link[=ak_install_specs]{ak_install_specs()}} — a CRAN name, or an
\code{org/repo} for the GitHub-only packages.}

\item{upgrade}{Logical. Let \pkg{pak} upgrade what is already installed.}
}
\value{
\code{TRUE} if an installer was available and ran, \code{FALSE} if not,
invisibly.
}
\description{
Split out from \code{\link[=load_packages]{load_packages()}} for two reasons: it is the only part that
touches the network, which keeps it out of the unit tests; and it is the only
part that can fail in ways worth reporting individually.
}
\details{
Never stops. Whether a failure matters is \code{\link[=load_packages]{load_packages()}}'s judgement to
make, from what is missing afterwards and whether any of it was required.
}
\keyword{internal}
