% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_pipeline.R
\name{ck_derived_map}
\alias{ck_derived_map}
\title{Combine the Derived-Column Maps into One}
\usage{
ck_derived_map(count_map = NULL, combination_map = NULL, exclusive_map = NULL)
}
\arguments{
\item{count_map}{The \code{map} from \code{\link{ck_add_selection_counts}}.}

\item{combination_map}{The \code{map} from
\code{\link{ck_add_choice_combinations}} run with \code{mode = "any"}.}

\item{exclusive_map}{The \code{map} from
\code{\link{ck_add_choice_combinations}} run with \code{mode = "only"}.}
}
\value{
A dataframe of \code{analysis_var}, \code{derived_column} and
\code{analysis_type}.
}
\description{
The selection counts, the choice combinations and the exclusive choice
combinations are all derived categorical columns that ride through the LOA as
plain proportions and are renamed afterwards. Everything downstream - the LOA
append, the rename, the block positioning, the separator rows - works off
this single table so no feature needs its own branch.
}
\keyword{internal}
