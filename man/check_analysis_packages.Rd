% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_local.R
\name{check_analysis_packages}
\alias{check_analysis_packages}
\title{What the Package Needs, and What Is Installed}
\usage{
check_analysis_packages()
}
\value{
A data frame with \code{package}, \code{need}, \code{installed}, \code{version},
\code{install} and \code{purpose}.
}
\description{
\code{need = "required"} means every run uses it and it is installed with the
package. \code{need = "optional"} means one feature needs it, and that feature is
the survey engine: an analysis row with a \code{level} set asks for a confidence
interval, which routes that row through \pkg{srvyr} and
\pkg{analysistools}. Leave every \code{level} cell empty and the fast tabulation
engine handles the whole run, needing neither - and, on a large
disaggregation, taking seconds rather than minutes.
}
\details{
Nothing is installed here. The two optional GitHub packages are not on CRAN,
so the \code{install} column gives the command to run.
}
\examples{
check_analysis_packages()
}
