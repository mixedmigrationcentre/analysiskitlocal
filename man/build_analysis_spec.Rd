% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{build_analysis_spec}
\alias{build_analysis_spec}
\title{Build an Analysis Specification from a List of Analysis Workbook}
\usage{
build_analysis_spec(workbook, dataset = NULL)
}
\arguments{
\item{workbook}{The list returned by \code{\link{read_loa_workbook}}, or a
bare named list of sheets.}

\item{dataset}{Optional dataset, with the ONA label row still on top.}
}
\value{
An object of class \code{analysis_spec}: a list with \code{loa},
\code{group_variables}, \code{rename_map}, \code{count_selections}, \code{count_combinations},
\code{exclude_choices}, \code{settings} and \code{problems}.
}
\description{
The single internal representation of a requested run. Everything downstream
reads this; nothing re-reads the workbook.
}
\details{
Variable names are resolved here: the workbook is written in raw dataset
codes throughout, and the spec comes back in post-rename names, matching the
dataset \code{\link{run_analysis_spec}} will hand to the pipeline.

This never stops. Inspect \code{problems} (or call
\code{\link{loa_has_errors}}) before running.
}
