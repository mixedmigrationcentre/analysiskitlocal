% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_pipeline.R
\name{ck_resolve_choices}
\alias{ck_resolve_choices}
\title{Match Choice Labels Against the Real Child Columns}
\usage{
ck_resolve_choices(
  parent,
  wanted,
  data_names,
  sm_separator = "/",
  ignore_case = TRUE
)
}
\arguments{
\item{parent}{The select_multiple parent variable name.}

\item{wanted}{Character vector of choice labels to find.}

\item{data_names}{Column names of the dataset.}

\item{sm_separator}{Separator between parent and choice. Default \code{"/"}.}

\item{ignore_case}{Logical. Match case-insensitively. Default \code{TRUE}.}
}
\value{
A list the same length as \code{wanted}, each element the child
column name(s) matching that label.
}
\description{
The choice labels handed to \code{count_combinations} are matched against the
actual select_multiple child column suffixes, after trimming and (by default)
case-insensitively. 4Mi labels are long free text - "Armed conflict,
generalised violence, and insecurity" - so a silent miss would produce a
table that looks fine and is wrong. This returns the empty set for a label
that matches nothing, and the caller is expected to fail on it.
}
\keyword{internal}
