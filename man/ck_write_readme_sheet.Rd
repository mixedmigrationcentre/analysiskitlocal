% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_write_readme_sheet}
\alias{ck_write_readme_sheet}
\title{Write the Branded Readme Sheet}
\usage{
ck_write_readme_sheet(
  wb,
  sheet,
  pal,
  font_name,
  composition = NULL,
  sheet_table,
  split_by = "none",
  hidden_columns = integer(0),
  hidden_names = character(0),
  dropped_groups = character(0),
  readme_text = NULL,
  pct_fmt = ck_percent_format(0)
)
}
\arguments{
\item{wb}{An openxlsx workbook.}

\item{sheet}{Readme sheet name (already added to the workbook).}

\item{pal}{Resolved MMC palette.}

\item{font_name}{Font used throughout.}

\item{composition}{Output of \code{\link[=ck_sample_composition]{ck_sample_composition()}}, or \code{NULL}.}

\item{sheet_table}{Data frame with \code{sheet} and \code{content}.}

\item{split_by}{The resolved \code{split_by} specification.}

\item{hidden_columns}{Identifier columns hidden on the table sheets.}

\item{hidden_names}{Names of those columns, used in the notes.}

\item{dropped_groups}{Group labels left out for having no respondents.}

\item{readme_text}{Optional character vector of extra lines.}

\item{pct_fmt}{Excel number format for proportions, from
\code{\link[=ck_percent_format]{ck_percent_format()}}. Used both for the sample-composition percentages
and for the number-format note, so the note cannot drift from the sheets.}
}
\value{
Invisibly \code{NULL}.
}
\description{
An MMC-branded cover sheet holding the sample composition across every
disaggregation variable, a directory of the sheets in the workbook, the
colour key and the reading notes.
}
\details{
This sheet is small and written once, so it uses \code{addStyle()} directly rather
than the style collector.
}
\keyword{internal}
