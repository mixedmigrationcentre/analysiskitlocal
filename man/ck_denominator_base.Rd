% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_denominator_base}
\alias{ck_denominator_base}
\title{Find the Denominator Column to Read Sample Sizes From}
\usage{
ck_denominator_base(blocks, total_columns = NULL)
}
\arguments{
\item{blocks}{The statistic blocks.}

\item{total_columns}{The \code{total_columns} argument, possibly \code{NULL}.}
}
\value{
A base name, or \code{NULL} when the blocks carry no count column at all.
}
\description{
The denominator can reach the formatter through either argument: as
\code{total_columns = c("n", "n_total")}, or folded into
\code{value_columns = c("stat", "n", "n_total")} with \code{total_columns} left \code{NULL}.
The block splitter cannot tell the two apart - it only counts columns - so
anything that reads sample sizes must not depend on which was used. It looks
at \code{total_columns} first, then at the base names the blocks actually hold.
}
\keyword{internal}
