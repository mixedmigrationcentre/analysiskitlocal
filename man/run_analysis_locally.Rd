% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_local.R
\name{run_analysis_locally}
\alias{run_analysis_locally}
\alias{print.analysis_run}
\title{Run an Analysis Locally, From Two Files to a Results Workbook}
\usage{
run_analysis_locally(dataset, loa, output_folder = NULL, verbose = TRUE, ...)

\method{print}{analysis_run}(x, ...)
}
\arguments{
\item{dataset}{A dataset path (\code{.csv} / \code{.xlsx}), the list from
\code{\link[=read_analysis_dataset]{read_analysis_dataset()}}, or a data frame with the ONA label row still on
top.}

\item{loa}{A List of Analysis path, or the list from \code{\link[=read_loa_workbook]{read_loa_workbook()}}.
A \code{.csv} carries no sheets and is read as the \code{analysis} table alone, so a
configured run needs \code{.xlsx}.}

\item{output_folder}{Folder to write the results workbook into. It must
already exist and be writable, and both are checked before the analysis
starts. \code{NULL} runs the analysis and writes nothing.}

\item{verbose}{Logical. Print the pipeline's progress as it arrives. The log
is kept in the result either way.}

\item{...}{Ignored.}

\item{x}{An \code{analysis_run} object.}
}
\value{
An object of class \code{analysis_run}: a list with \code{results} (the full
pipeline result), \code{spec}, \code{dataset}, \code{coverage}, \code{problems},
\code{output_path}, \code{log}, \code{elapsed} and \code{save_error}. Print it for a summary.
}
\description{
The whole Analysis Kit workflow as one call: read the dataset and the List
of Analysis, check them against each other, run every analysis the workbook
asks for across every disaggregation it declares, and write the results to
an MMC-branded Excel workbook.
}
\section{What it will not do}{


The run \strong{stops before doing any work} when the List of Analysis has a
fatal problem, and the error names every one of them. That is deliberate:
the alternative is a workbook full of plausible numbers attributed to the
wrong disaggregation. Call \code{\link[=check_analysis_inputs]{check_analysis_inputs()}} first to see the
problems without triggering the run.

Warnings do not stop a run - they mean less output, not wrong output - and
they arrive in \verb{$log}, which is worth reading rather than skipping. A
grouping variable absent from the dataset, a \verb{Don't know} label that matched
nothing, a town set aside for having four interviews: all of them are
warnings, and all of them change what is in the file.
}

\section{The output file}{


Named \verb{analysiskit_<dataset stem>_<YYYYmmdd-HHMMSS>.xlsx}, and an existing
file is never overwritten - a second run in the same second gets a \verb{_1}
suffix. The workbook's readme sheet records the dataset, the List of
Analysis, the disaggregations and every setting that was applied, so a file
found six months later can be traced back to what produced it.

Pass \code{output_folder = NULL} to run without writing anything. The results are
returned either way.
}

\examples{
\dontrun{
# The usual case
run <- run_analysis_locally(
  dataset       = "data/mmr_analysis_data_25_26.xlsx",
  loa           = "resources/loa_template.xlsx",
  output_folder = "output"
)

run                      # summary, including the saved path
run$output_path          # the workbook that was written
run$log                  # what the pipeline reported
run$results$combined_results[1:5, 1:6]

# Check first, run second
checks <- check_analysis_inputs("data/export.xlsx", "resources/loa.xlsx")
if (checks$ready) {
  run <- run_analysis_locally(checks$dataset, "resources/loa.xlsx", "output")
}

# Analyse without writing anything
run <- run_analysis_locally("data/export.xlsx", "resources/loa.xlsx")

# The same List of Analysis over a folder of exports
runs <- lapply(
  list.files("data", pattern = "[.]xlsx$", full.names = TRUE),
  run_analysis_locally,
  loa = "resources/loa.xlsx",
  output_folder = "output"
)
}
}
\seealso{
\code{\link[=check_analysis_inputs]{check_analysis_inputs()}} to look before you leap,
\code{\link[=run_analysis_spec]{run_analysis_spec()}} to run an already-built specification,
\code{\link[=run_group_analysis_pipeline]{run_group_analysis_pipeline()}} to bypass the workbook entirely.
}
