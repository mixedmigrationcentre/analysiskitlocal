% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{apply_rename_map}
\alias{apply_rename_map}
\title{Apply a Rename Map to a Dataset}
\usage{
apply_rename_map(dataset, map, sm_separator = "/")
}
\arguments{
\item{dataset}{A data frame.}

\item{map}{A named character vector: names are the current names, values the
new ones.}

\item{sm_separator}{Select_multiple separator.}
}
\value{
\code{dataset} with its columns renamed.
}
\description{
Renames the column itself \strong{and every select_multiple child} sharing the
\verb{<old><sm_separator>} prefix. Renaming \code{Q78} without rewriting
\verb{Q78/Economic reasons} would sever the parent-child link, and every
select_multiple analysis of that question would silently return nothing.
}
\details{
The whole map is applied in one pass over the original names, so a map that
happens to rename \code{A} to \code{B} and \code{B} to \code{C} cannot chain \code{A} through to \code{C}.
}
