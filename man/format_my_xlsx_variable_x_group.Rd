% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{format_my_xlsx_variable_x_group}
\alias{format_my_xlsx_variable_x_group}
\title{Format XLSX Variable by Group Using MMC Branding}
\usage{
format_my_xlsx_variable_x_group(
  table_group_x_variable,
  file_path = NULL,
  table_name = "variable_x_group_table",
  value_columns = c("stat", "stat_low", "stat_upp"),
  total_columns = NULL,
  readme_sheet_name = "readme",
  table_sheet_name = "variable_x_group_table",
  overwrite = FALSE,
  layout = c("matrix", "blocks"),
  insert_empty_rows = FALSE,
  empty_rows_col = "analysis_var",
  sort_within_groups = TRUE,
  split_by = "group_variable",
  max_sheets = 60,
  split_by_group_variable = NULL,
  column_map = NULL,
  repeat_overall = TRUE,
  order_groups_by_n = TRUE,
  order_groups_per_question = TRUE,
  drop_empty_groups = TRUE,
  short_group_labels = TRUE,
  colour_scale = TRUE,
  max_colour_scale_rules = 250,
  round_digits = NULL,
  percent_digits = 0,
  hidden_columns = 1:3,
  index_width = 7,
  stat_width = 13,
  total_width = 5,
  palette = NULL,
  font_name = "Arial Narrow",
  readme_text = NULL,
  verbose = TRUE
)
}
\arguments{
\item{table_group_x_variable}{A data frame, a named list containing
\code{table_name}, or the list returned by \code{run_group_analysis_pipeline()}
(which supplies both \code{combined_results} and \code{column_map}).}

\item{file_path}{Output path. \code{NULL} (default) returns the workbook.}

\item{table_name}{Name of the element holding the table when a list is
supplied. \code{"combined_results"} is picked up automatically.}

\item{value_columns}{Statistic column prefixes.}

\item{total_columns}{Count column prefixes (e.g. \code{c("n", "n_total")}),
formatted as whole numbers.}

\item{readme_sheet_name}{Name of the readme sheet.}

\item{table_sheet_name}{Name of the table sheet; used only when a sheet name
cannot be derived.}

\item{overwrite}{Logical, overwrite an existing file.}

\item{layout}{\code{"matrix"} (default) writes the wide variable-by-group table:
one row per question option, one block of columns per disaggregation group.
\code{"blocks"} writes one small table per question - the question on top, the
disaggregation variable band under it, then a header row naming each group
with its sample size, then the answer categories - with the percentages on
the left of the sheet and the matching counts on the right.}

\item{insert_empty_rows}{Logical. Insert a blank row between question groups.}

\item{empty_rows_col}{Column used for grouping when inserting empty rows.}

\item{sort_within_groups}{Logical, sort rows inside each group by the first
\code{stat_} column, descending.}

\item{split_by}{How results are spread across sheets. Any combination of
\code{"none"}, \code{"group_variable"} (one sheet per disaggregation variable, needs
\code{column_map}) and any column name (one sheet per value of that column - use
for \code{sector}, \code{indicator}, \code{analysis_type} or any LOA column carried
through by \code{extra_columns}). \code{c("sector", "group_variable")} writes one
sheet per sector and grouping variable. \code{"analysis_var"} and
\code{"analysis_var_value"} are accepted as aliases of the renamed \code{question}
and \code{option} columns.}

\item{max_sheets}{Refuse to build more sheets than this, so a typo in
\code{split_by} cannot produce hundreds of sheets.}

\item{split_by_group_variable}{Deprecated logical kept for backwards
compatibility. \code{TRUE} maps to \code{split_by = "group_variable"}, \code{FALSE} to
\code{"none"}. Ignored when \code{split_by} is given explicitly.}

\item{column_map}{Optional data frame with \code{group_variable} and \code{column}, as
returned by \code{run_group_analysis_pipeline()}.}

\item{repeat_overall}{Logical. Repeat the Overall block on every sheet.}

\item{order_groups_by_n}{Logical, default \code{TRUE}. Order the group columns
within each grouping variable by sample size, largest first, instead of the
alphabetical order they come out of the pipeline in - so
\verb{Female (n=740), Male (n=1152)} is written \verb{Male (n=1152), Female (n=740)},
and \verb{No, Refused, Yes} becomes \verb{Yes, No, Refused}. Only blocks belonging to
the same grouping variable move, so the merged variable band above them
still spans a contiguous stretch and \code{Overall} stays put. Needs a count
column: pass \code{total_columns} (e.g. \code{c("n", "n_total")}) or there is nothing
to sort by and the order is left alone. \code{FALSE} restores the alphabetical
order.

This sets the sheet-wide order, from each group's sample size across the
whole table. In \code{layout = "blocks"} it is then refined per question unless
\code{order_groups_per_question} is turned off.}

\item{order_groups_per_question}{Logical, default \code{TRUE}, \code{layout = "blocks"}
only. Give every question its own group order, largest first by that
question's own denominator, rather than having them all follow the
sheet-wide order. Questions with different coverage then show their groups
in different orders, which is the point.

The permutation stays inside each grouping variable's run, so the merged
variable band above the columns, the alternating block shading and the
column widths are all unaffected - only which block sits in which column
changes, and every header carries its own name and \verb{(n=)}.

The cost: reading straight down a column no longer follows one group. The
third column may be \code{Male} on one question and \code{Female} on the next. Set
this to \code{FALSE} if you need the columns to line up down the sheet;
\code{layout = "matrix"} always does, since there every question is a row under
one shared header.}

\item{drop_empty_groups}{Logical, default \code{TRUE}. Leave out any group whose
sample size is zero on that sheet, in both the percentage panel and the
count panel. A grouping variable's levels are fixed across the whole table,
so a country with no interviews for the questions on a sheet would
otherwise get a full block of empty (or \verb{#NUM!}) columns. Judged per sheet
rather than per question, so the group columns stay aligned down the sheet;
the Overall block is never dropped. Anything dropped is listed in a readme
note, since silently omitting a country from a table is easy to misread.}

\item{short_group_labels}{Logical. In \code{layout = "blocks"}, drop the
disaggregation variable prefix from each group label, so
\code{Respondent_Gender_Female (n=11422)} reads \code{Female (n=11422)} under the
\code{Respondent_Gender} band. \code{Overall} is never touched.}

\item{colour_scale}{Logical. Apply the per-question colour scale to the first
statistic column.}

\item{max_colour_scale_rules}{Skip the colour scale when it would need more
rules than this on a sheet (hundreds of rules make the workbook slow to
open in Excel).}

\item{round_digits}{Optional integer. Round numeric statistic columns before
writing. Shortens the XML noticeably on very large tables.}

\item{percent_digits}{Decimal places shown on proportions. \code{0} (the default)
displays \code{0.668039538714992} as \verb{67\%}. This is a display format, so the
cell keeps its full precision and the figures still add up correctly.
Means and medians are unaffected; they keep two decimals.}

\item{hidden_columns}{Identifier columns to hide on every table sheet.
Defaults to the first three (machine names and analysis type), which are
kept in the file but collapsed out of the way. \code{NULL} shows everything.}

\item{index_width}{Width of the visible identifier columns.}

\item{stat_width}{Width of the statistic columns.}

\item{total_width}{Width of the count columns named in \code{total_columns}. Kept
narrow so the statistics of neighbouring disaggregations sit side by side.}

\item{palette}{Optional named vector of colour overrides. See \code{\link[=mmc_colours]{mmc_colours()}}.}

\item{font_name}{Font used throughout.}

\item{readme_text}{Optional character vector of extra readme lines.}

\item{verbose}{Logical, print progress messages.}
}
\value{
An \code{openxlsx} workbook if \code{file_path} is NULL, otherwise the file is
written and the path returned invisibly.
}
\description{
Writes an analysis table to an Excel workbook in a variable-by-group layout
styled with the MMC palette: a navy (\verb{#003D58}) band for the merged
disaggregation-group headers, a teal (\verb{#00A2A5}) band for the column headers,
a light blue index panel on the left for the question / option columns, and
alternating white / pale blue shading per block of statistics.
}
\details{
Built for the wide outputs of \code{run_group_analysis_pipeline()}, which can run
to thousands of columns when many countries or towns are used as grouping
variables. With \code{column_map} from the pipeline, each grouping variable is
written to its own sheet, repeating the index panel and, optionally, the
Overall block.
}
