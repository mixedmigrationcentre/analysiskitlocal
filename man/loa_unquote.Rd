% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{loa_unquote}
\alias{loa_unquote}
\title{Take a Quoted Setting Verbatim}
\usage{
loa_unquote(value)
}
\arguments{
\item{value}{The trimmed cell value.}
}
\value{
The value, unwrapped when it was quoted.
}
\description{
A spreadsheet will not carry a leading or trailing space: writing \code{ only} in
a cell and reading it back gives \code{only}, and the label then renders
"Economiconly". The loss happens in the file format, before this reader sees
the value, so no amount of not-trimming here recovers it.
}
\details{
So a value wrapped in double quotes is taken exactly as written between them.
\code{" only"} survives the round trip; \code{only} behaves as before. This applies to
every text setting, not a special list of them, because any of them could
need an edge space.
}
\keyword{internal}
