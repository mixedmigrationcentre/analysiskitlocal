% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/checks.R
\name{check_analysis_inputs}
\alias{check_analysis_inputs}
\alias{print.analysis_readiness}
\title{Check a Dataset and a List of Analysis Against Each Other}
\usage{
check_analysis_inputs(dataset, loa, output_folder = NULL)

\method{print}{analysis_readiness}(x, ...)
}
\arguments{
\item{dataset}{A dataset path, the list from \code{\link[=read_analysis_dataset]{read_analysis_dataset()}}, or a
plain data frame with the ONA label row still on top.}

\item{loa}{A List of Analysis path, or the list from
\code{\link[=read_loa_workbook]{read_loa_workbook()}}.}

\item{output_folder}{Optional folder the results would be written to. When
given it is checked for existence and write access now, so a long run is
never lost to a read-only destination.}

\item{x}{An \code{analysis_readiness} object.}

\item{...}{Ignored.}
}
\value{
An object of class \code{analysis_readiness}: a list with \code{ready},
\code{problems}, \code{counts}, \code{coverage}, \code{sheets}, \code{spec}, \code{dataset} and
\code{folder_problem}. Print it for a summary.
}
\description{
Everything \code{\link[=run_analysis_locally]{run_analysis_locally()}} checks before it commits to a run, with
nothing run. Use it to see what a workbook would do to a dataset - and what
it would refuse to do - without waiting for an analysis.
}
\details{
\strong{This never stops.} A workbook with fatal problems comes back described
rather than thrown, so every problem is visible at once instead of one per
attempt. \verb{$ready} is the answer to "would a run go ahead".

The severity rule, inherited from \code{validate_loa()}: \strong{fatal} when the run
would produce wrong or misleading output, \strong{warning} when it would only
produce less output. So a grouping variable that is missing from the dataset
warns - one fewer column block - while two \code{new_name} values where one
contains the other is fatal, because the column map would then attribute
columns to the wrong disaggregation.
}
\examples{
\dontrun{
checks <- check_analysis_inputs(
  dataset = "data/mmr_analysis_data_25_26.xlsx",
  loa     = "resources/loa_template.xlsx"
)

checks                              # the summary
ak_problems_display(checks$problems) # every problem, errors first
subset(checks$coverage, !present)    # variables the dataset does not have
}
}
\seealso{
\code{\link[=validate_loa]{validate_loa()}} for the checks themselves,
\code{\link[=loa_variable_coverage]{loa_variable_coverage()}} for the per-reference variable table.
}
