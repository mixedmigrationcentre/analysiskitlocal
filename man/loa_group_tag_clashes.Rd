% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{loa_group_tag_clashes}
\alias{loa_group_tag_clashes}
\title{Find Grouping Variables Whose Output Columns Would Be Misattributed}
\usage{
loa_group_tag_clashes(
  group_variables,
  dataset = NULL,
  raw_names = NULL,
  missing_group_label = "Missing"
)
}
\arguments{
\item{group_variables}{Grouping variables, excluding \code{Overall}.}

\item{dataset}{Optional dataset, used to test the real levels.}

\item{raw_names}{Optional named vector mapping each grouping variable to the
raw dataset column its levels come from.}

\item{missing_group_label}{Label used for respondents missing on a grouping
variable.}
}
\value{
A data frame with \code{group_variable}, \code{column} and \code{attributed_to}.
}
\description{
The wide table names its columns \verb{stat_<group>_<value>} and the \code{column_map}
then works out which disaggregation each column belongs to by looking for
\verb{_<group>_}, first match wins. Two consequences, both silent:
}
\details{
\itemize{
\item a grouping variable whose name is contained in another's - \code{Region}
and \code{Region_of_origin} - takes ownership of the other's columns;
\item a grouping variable with a level literally called \code{Overall}, or a
level that completes another variable's tag, lands in the wrong block.
}

The table itself is still correct; the map that says what the columns mean
is not. That is why this is fatal rather than a warning.
}
\keyword{internal}
