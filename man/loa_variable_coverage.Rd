% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_loa.R
\name{loa_variable_coverage}
\alias{loa_variable_coverage}
\title{Every Dataset Variable the Workbook Names, and Whether It Is There}
\usage{
loa_variable_coverage(workbook, dataset, sm_separator = NULL)
}
\arguments{
\item{workbook}{The list from \code{\link{read_loa_workbook}}, or a bare
named list of sheets.}

\item{dataset}{The dataset, with the ONA label row still on top.}

\item{sm_separator}{Select_multiple separator. Read from the \code{settings}
sheet when not given.}
}
\value{
A data frame with \code{variable}, \code{role}, \code{sheet}, \code{row} and \code{present},
one row per reference.
}
\description{
The check that runs the moment both files are in: each variable the workbook
refers to, where it is referred to, and whether the uploaded dataset
actually has it. Presence is judged by \code{\link{loa_var_present}}, so a
select_multiple parent counts when its child columns are present even though
ONA did not export the concatenated parent column.
}
\details{
Names are matched as written in the workbook - raw dataset codes - against
the raw dataset, before any renaming.
}
