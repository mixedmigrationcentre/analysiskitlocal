% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/format_xlsx.R
\name{ck_order_blocks_by_size}
\alias{ck_order_blocks_by_size}
\title{Order Group Blocks by Sample Size Within Each Grouping Variable}
\usage{
ck_order_blocks_by_size(blocks, block_group, size)
}
\arguments{
\item{blocks}{The statistic blocks.}

\item{block_group}{Grouping variable of each block.}

\item{size}{Sample size of each block, from \code{\link[=ck_block_sample_sizes]{ck_block_sample_sizes()}}.}
}
\value{
An integer permutation of \code{seq_along(blocks)}.
}
\description{
Group values come out of the pipeline alphabetically, which puts \code{Female}
before \code{Male} and \verb{No, Refused, Yes} in that order regardless of how many
respondents are behind each. Sorting them largest first puts the substantial
columns where they will be read.
}
\details{
Only blocks \emph{within} the same grouping variable move, so the merged variable
band above them still spans a contiguous stretch and \code{Overall} - a run of one
\itemize{
\item stays where it is. The order is decided once for the whole workbook from
each block's overall sample size, not per question: the block layout writes
one header per question, and re-sorting each of them independently would
leave the columns unaligned down the sheet.
}
}
\keyword{internal}
