% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_results.R
\name{ak_prepare_for_export}
\alias{ak_prepare_for_export}
\title{Prepare the Wide Table for the Formatter}
\usage{
ak_prepare_for_export(wide, layout = c("blocks", "matrix"))
}
\arguments{
\item{wide}{The \code{combined_results} table.}

\item{layout}{The formatter layout the table is headed for.}
}
\value{
\code{wide}, ready to format.
}
\description{
Two things about the pipeline's wide table need attention before it is
formatted, and both come from the separator rows.
}
\details{
\code{ck_insert_count_separators()} adds a blank spacer and a heading above each
derived block, marked in a \code{row_type} column that it appends at the \emph{end} of
the table. Left alone:

\itemize{
\item the formatter reads everything after the first \code{stat_} column as a
statistic, so a trailing \code{row_type} is reported as an unrecognised
column and dropped with a warning;
\item the marker rows carry no \code{sector}, so splitting sheets by sector
sends them all to a sheet of their own called "not specified".
}

The spacer and heading exist to separate a derived block from the choice
rows above it in the \strong{matrix} layout, where everything is one continuous
table. The \strong{blocks} layout already gives every question its own titled
table, so there the markers are redundant and are removed; in the matrix
layout they are kept and \code{row_type} is moved in among the identifier
columns, where the formatter expects identifiers to be.
}
