% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_results.R
\name{ak_export_settings}
\alias{ak_export_settings}
\title{Export Settings for a Completed Run}
\usage{
ak_export_settings(results, spec)
}
\arguments{
\item{results}{The list returned by \code{run_group_analysis_pipeline()}.}

\item{spec}{The \code{analysis_spec} behind the run.}
}
\value{
A named list of arguments for \code{format_my_xlsx_variable_x_group()}.
}
\description{
Derived from the run rather than hard-coded, so the split stays correct when
the workbook asks for different \code{value_columns}. With the defaults this
reproduces the settings behind \verb{4Mi_results_QN6.xlsx}: the blocks layout, one
sheet per sector, \code{stat} as the statistic and \code{n} / \code{n_total} as the counts.
}
\details{
\code{length(c(value_columns, total_columns))} must equal the number of columns
the pipeline wrote per group block, or the formatter cuts the blocks in the
wrong places. Splitting one list guarantees that by construction.
}
