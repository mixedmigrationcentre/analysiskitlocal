# The Steps of the Workflow

The Steps of the Workflow

## Usage

``` r
ak_steps(destination_label = "Output folder")
```

## Arguments

- destination_label:

  Label for the fourth step.

## Value

A named character vector: step id to label.
