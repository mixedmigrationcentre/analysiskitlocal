# Analysis Types the Pipeline Produces Rather Than Accepts

These appear in the output, built from the `count_selections` and
`count_combinations` sheets. Writing one into the `analysis` sheet
aborts the run, so it earns its own message.

## Usage

``` r
loa_derived_analysis_types()
```

## Value

A character vector.
