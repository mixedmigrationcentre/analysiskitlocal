# Build the exclude_choices Vector from its Sheet

Build the exclude_choices Vector from its Sheet

## Usage

``` r
loa_exclude_choices(sheet)
```

## Arguments

- sheet:

  The `exclude_choices` data frame.

## Value

A character vector.
