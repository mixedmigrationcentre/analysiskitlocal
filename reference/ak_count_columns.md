# Columns That Are Counts Rather Than Statistics

The formatter needs the statistic prefixes and the count prefixes
separately: counts are formatted as whole numbers, kept narrow, and are
what the sample sizes and the empty-group test are read from.

## Usage

``` r
ak_count_columns()
```

## Value

A character vector.
