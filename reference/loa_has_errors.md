# Does a Problems Table Contain a Fatal Problem

Does a Problems Table Contain a Fatal Problem

## Usage

``` r
loa_has_errors(problems)
```

## Arguments

- problems:

  A problems data frame.

## Value

`TRUE` when at least one row has severity `"error"`.
