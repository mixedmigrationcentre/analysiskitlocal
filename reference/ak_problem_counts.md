# Count Problems by Severity

Count Problems by Severity

## Usage

``` r
ak_problem_counts(problems)
```

## Arguments

- problems:

  A problems data frame, or `NULL`.

## Value

A named integer vector with `error` and `warning`.
